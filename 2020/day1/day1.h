#pragma once

#include <string>
#include <vector>

class day1 {
 public:
  static uint64_t expense(std::vector<uint64_t> values);
  static uint64_t expense3(std::vector<uint64_t> values);
 private:
};